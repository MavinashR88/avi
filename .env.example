# Avista — local environment template
# Copy to .env and fill in. NEVER commit .env (see .gitignore).

# LLM providers (per foundation.md §6). Not yet wired into the app code.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Observability (per foundation.md §4). Not yet wired.
SENTRY_DSN=
OTEL_EXPORTER_OTLP_ENDPOINT=

# Next.js
NEXT_TELEMETRY_DISABLED=1
